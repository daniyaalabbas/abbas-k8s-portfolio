kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: multinode

nodes:
- role: control-plane
  extraPortMappings:
  - containerPort: 30007
    hostPort: 30007
    protocol: TCP

- role: worker
- role: worker
- role: worker

